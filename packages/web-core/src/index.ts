// @billfree/web-core — framework-agnostic frontend core:
// domain types, pure utils, and shared constants. No React.
//
// Consumers import from the package root:
//   import { normaliseTicket, STATUS_COLORS, type Ticket } from '@billfree/web-core';

export * from './types';
export * from './constants';
export * from './utils';
