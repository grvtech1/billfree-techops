nameOverride: ticket-service
image:
  repository: ghcr.io/grvtech1/billfree-techops/ticket-service
  tag: "b7fe79c0f4ef43e1da64e8ba9aec5d5776dc8570"
replicaCount: 2
containerPort: 8080
env:
  - name: SERVICE_NAME
    value: ticket-service
  - name: PORT
    value: "8080"
envFrom:
  # JWT_SECRET, JWT_ISSUER, DATABASE_URL (bootstrap secret — see deploy README).
  - secretRef:
      name: billfree-app-secrets
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 70



