apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: report-service
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/grvtech1/billfree-techops.git
      targetRevision: main
      path: deploy/charts/microservice
      helm:
        releaseName: report-service
        valueFiles:
          - $values/deploy/apps/report-service/values.yaml
    - repoURL: https://github.com/grvtech1/billfree-techops.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: billfree
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
