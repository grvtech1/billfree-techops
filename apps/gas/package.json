{
  "name": "@billfree/gas-backend",
  "version": "10.0.0",
  "private": true,
  "description": "BillFree TechSupport Ops — modular Google Apps Script backend (JSON API + Sheets data layer).",
  "scripts": {
    "test": "node tests/run.js",
    "push": "clasp push",
    "deploy": "clasp push && clasp deploy --description \"$npm_config_desc\"",
    "open": "clasp open",
    "pull": "clasp pull",
    "logs": "clasp logs --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
